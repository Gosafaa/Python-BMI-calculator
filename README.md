# Python-BMI-calculator

A simple command-line Body Mass Index (BMI) calculator built with Python.

## Features

- Calculates Body Mass Index (BMI)
- Classifies BMI according to standard BMI categories
- Validates user input
- Prevents invalid values (such as zero or negative numbers)
- Allows users to perform multiple calculations

